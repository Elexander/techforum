<%= form_for(@privatemessage) do |f| %>
  <% if @privatemessage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@privatemessage.errors.count, "error") %> prohibited this private message from being saved:</h2>

      <ul>
      <% @privatemessage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :recipent %><br />
    <%= f.text_field :recipent %>
  </div>
  <div class="field">
    <%= f.label :subject %><br />
    <%= f.text_field :subject %>
  </div>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
