 <!-- -->

<div class="container">
  <div class="row">
    <div class="col s5 m10 offset-m1">
      <% @discussion_posts.each do |post| %>
      <div class="card-panel grey lighten-5 z-depth-1 hoverable">
        <span class="black-text">
          <%= link_to post.title, { :action=>"index", :controller=>"posts", :post_id => post.id} %>
          <%= post.content%><br>
          <stron><%= post.user.name %></strong>
        </span>
      </div>
      <% end %>
    </div>
  </div>
</div>





<!-- <center>
  <table width="600">
    <thead >
      <tr>
        <th>User</th>
        <th>Title</th>
        <!-- <th>Content</th>
      </tr>
    </thead>

    <tbody>
      <% @post.each do |post| %>
        <tr  align = "center">
          <td><%= post.user.email %></td>
          <td><%= link_to post.title , {:action=>"index", :controller=>"comments", :post_id => post.id} %></td>
          
          <!-- <td><%= post.content %></td> 
        </tr>
      <% end %>
    </tbody>
  </table>
  <%= link_to "Create Post" , {:action=>"new", :controller=>"posts"} %>
</center>   -->